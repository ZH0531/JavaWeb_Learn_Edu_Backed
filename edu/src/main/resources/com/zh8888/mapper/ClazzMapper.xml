<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh8888.mapper.ClazzMapper">
    <insert id="insert">
        insert into clazz (name, room, begin_date, end_date, master_id, subject, create_time, update_time)
        values (#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, #{createTime}, #{updateTime})
    </insert>
    <update id="updateClazz">
        update clazz
        <set>
            <if test="name != null and name != '' ">
                name = #{name},
            </if>
            <if test="room != null">
                room = #{room},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null">
                end_Date = #{endDate},
            </if>
            <if test="masterId != null and masterId != '' ">
                master_id = #{masterId},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteById">
        delete from clazz where id = #{id}
    </delete>

    <select id="page" resultType="com.zh8888.pojo.entity.Clazz">
        select clazz.*, emp.name masterName
        from clazz
        left join emp on clazz.master_id = emp.id
        <where>
            <if test="name != null and name != '' ">
                clazz.name like concat('%', #{name}, '%')
            </if>

            <if test="begin != null and end!=null">
                and clazz.end_date between #{begin} and #{end}
            </if>
        </where>
        order by clazz.update_time desc

    </select>
    <select id="getClazzById" resultType="com.zh8888.pojo.entity.Clazz">
        select clazz.*, emp.name masterName
        from clazz
        left join emp on clazz.master_id = emp.id
        where clazz.id = #{id}
    </select>
    <select id="getStudentCountById" resultType="java.lang.Integer">
        select count(*) from student where clazz_id = #{id}
    </select>


</mapper>
